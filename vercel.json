{
  "buildCommand": "vite build --config vite.config.vercel.ts",
  "outputDirectory": "dist",
  "framework": null,
  "routes": [
    { "handle": "filesystem" },
    { "src": "^/api/(.*)", "dest": "/api/$1" },
    { "src": ".*", "dest": "/index.html" }
  ]
}
